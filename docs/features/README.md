# docs/features/

此目录存放功能级设计文档。

每个新功能在实现前，必须在此目录下创建设计文档：
`docs/features/<feature-slug>.md`

设计文档模板见 AGENTS.md → Phase 1 设计输出。

工单号、安全扫描、整改过程这类私有材料不要放在这里，统一放本地私有目录 `docs/private/`（已加入 `.gitignore`）。
