# 1.2 用户登录（密码登录 + JWT 签发 + Refresh 轮换 + Logout）

## 目标

实现密码登录三件套：

| Endpoint | 用途 |
|---|---|
| `POST /api/v1/auth/login` | identifier + password 验证；签发 access(2h) + refresh(7d) |
| `POST /api/v1/auth/refresh` | 拿 refresh 换新 access + 新 refresh（轮换） |
| `POST /api/v1/auth/logout` | 主动吊销 refresh，立即让该设备失效 |

完成后调用方能完整跑：注册 → 登录 → 拿到 access token → 主动登出/或自动 refresh。

**明确边界**：

- 1.2 **不**做 protected endpoint（access token 怎么用归 1.3 JWT 中间件）。1.2 只签发 + 验证 token，不强制要求其他 endpoint 鉴权。
- 1.2 **不**做忘记密码 / 改密码 / 第三方登录 / 二次验证 / 设备指纹 / 登录限流。
- 1.2 **不**做"全设备登出"（吊销该用户所有 refresh）—— 单 jti 单 key 删除即可，全设备登出留给 1.4 资料修改密码触发。
- access token 的负载只放最小化身份字段（`sub` + 标准 claims），具体业务字段（昵称/角色）由 1.3 中间件解 token 后从 DB / 缓存补，不塞 JWT。

## 设计

### 架构决策引用

- [ADR-001](../adr/001-modular-monolith.md)：登录走 `internal/module/user/`，路由在 `internal/server/router.go` 注册。
- [ADR-002](../adr/002-dual-database.md)：用户查询走 MySQL（交易型）。
- [ADR-003](../adr/003-redis-unified-cache.md)：refresh token 哈希存 Redis `PoolSession`（DB3，`RequiresTTL=true`）。

### 数据模型

#### MySQL：复用 1.1 的 `users` 表

1.2 新增的查询能力：

- `UserStore.FindByIdentifier(ctx, identifier, idType string) (*User, error)`：登录路径用，按 idType 单列精确查询
- `UserStore.FindByID(ctx, id uint64) (*User, error)`：refresh 路径用，复查 `user.Status`，确保被禁用账号即使 token 仍未过期也不能继续轮换

**形态识别复用 1.1 同一 helper**（保证注册和登录的口径绝对一致，避免一边能注册一边却查不到）：

- 在 `internal/module/user/service.go` 把 1.1 现有 `normalizeAndValidate` 的"识别 + 规范化"那段抽出独立 helper：

  ```go
  // identifierKind 区分 username / phone / email；与 1.1 同正则 + ParseAddress 严格校验。
  type identifierKind string
  const (
      kindUsername identifierKind = "username"
      kindPhone    identifierKind = "phone"
      kindEmail    identifierKind = "email"
  )

  // classifyIdentifier 接收原始 identifier，返回 (规范化后字符串, kind, error)。
  // - 对 1.1 注册：调用方拿 (norm, kind) 写入对应列
  // - 对 1.2 登录：调用方拿 (norm, kind) 传给 store.FindByIdentifier
  // 拒绝条件与 1.1 完全相同：lower-case 后 username 走 `^[a-z0-9_-]{4,32}$` **且至少一个非数字字符**
  // （v3.2 修订加的硬约束，避免与 phone 数值空间撞值）；phone 走纯数字 6-20 位；email 走严格正则 + ParseAddress。
  func classifyIdentifier(s string) (string, identifierKind, error)
  ```

- `normalizeAndValidate` 内部改成调 `classifyIdentifier`；外部行为不变，单测全部通过。
- `service.Login` 调用 `classifyIdentifier(req.Identifier)` 拿到 `(norm, kind)`，再传给 `UserStore.FindByIdentifier(ctx, norm, string(kind))`。

DAO 层 `FindByIdentifier`：按 `idType` 决定查哪一列（`WHERE username = ?` / `WHERE phone = ?` / `WHERE email = ?`），**只查命中的那列**——避免 `OR` 三列让 MySQL 失去索引选择能力。

未命中返回新 sentinel `ErrUserNotFound`。

#### Redis：refresh token 存储

- **Pool**：`PoolSession`（DB3）
- **Key 模式**：`user:refresh:<uid>:<jti>`（由 `internal/cache/keys.go` 中新增的 `UserRefreshKey(uid, jti)` helper 生成；refresh_store.go 不自定义 pattern，集中到 keys.go 是为了配合 change-impact 同步规则）
  - `<uid>` 用户 ID（uint64 字符串）
  - `<jti>` 16 字节 `crypto/rand` 生成的 base32-nopad 字符串（26 字符，安全随机），登录时生成、refresh 时换新
- **Value**：refresh token 原文的 SHA-256 十六进制（64 字符）+ 元数据 JSON：
  ```json
  {
    "token_hash": "<sha256-hex>",
    "issued_at":  1716000000
  }
  ```
  1.2 仅保留 `token_hash` 和 `issued_at`，**不存** user_agent / IP（高基数 + 来源不可信 + 长度边界没法在 1.2 一次性定）。后续阶段如需多设备审计能力，单独走 ADR 或 1.x 子项加，规划时一并定截断/脱敏策略。
- **TTL**：`cfg.Auth.RefreshTokenTTL`（默认 7d / 168h）。Redis `SETEX` 一次设置，到期自动消失。
- **多设备**：每次登录生成独立 `<jti>`，多个 key 并存。

**为什么哈希 token 而不是直接对比**：泄露 Redis 数据时不能直接拿到能用的 refresh token。SHA-256 对 refresh token 这种高熵随机串足够（无需 bcrypt——成本太高，refresh 是热路径），原文带 jti 客户端用，验证时 `SHA-256(token) == stored_hash` 比对。

### JWT Claims

```go
// AccessClaims（access token）
{
  "iss": "vibeshop",
  "sub": "<user_id>",      // 字符串形式 uint64
  "iat": <unix>,
  "exp": <unix + 2h>,
  "typ": "access"
}

// RefreshClaims（refresh token）
{
  "iss": "vibeshop",
  "sub": "<user_id>",
  "jti": "<base32-26char>",  // 与 Redis key 的 <jti> 一致
  "iat": <unix>,
  "exp": <unix + 7d>,
  "typ": "refresh"
}
```

- 算法：HS256（对称密钥从 `cfg.Auth.JWTSecret` 读取，不少于 32 字节，启动校验）
- `typ` claim 防混用：access 不能当 refresh、反之亦然

### API 设计

#### `POST /api/v1/auth/login`

请求：

```json
{
  "identifier": "alice",
  "password":   "Strong#1"
}
```

入参：

- `identifier`：原文（不是 username/phone/email 三独立字段）。service 层 `TrimSpace`，再按形态识别（同 1.1 规范化规则）。
- `password`：8–72 字符。

成功响应（200）：

```json
{
  "user_id":           1,
  "access_token":      "eyJ...",
  "refresh_token":     "eyJ...",
  "access_expires_at":  "2026-05-17T18:00:00Z",
  "refresh_expires_at": "2026-05-24T16:00:00Z",
  "token_type":        "Bearer"
}
```

错误：

| HTTP | code | 触发 |
|---|---|---|
| 400 | `INVALID_REQUEST` | identifier 空 / password 长度违规 / JSON 畸形 |
| 401 | `INVALID_CREDENTIALS` | 用户不存在 **OR** 密码错（**统一文案**，缓解枚举） |
| 500 | `INTERNAL` | 其他 |

#### `POST /api/v1/auth/refresh`

请求：

```json
{ "refresh_token": "eyJ..." }
```

服务端流程：

1. 解析 + 验证签名 + `exp` 未过期 + `typ == "refresh"`
2. 从 claims 取 `sub` 和 `jti`，组**旧 key** `user:refresh:<sub>:<jti>`
3. 生成新 jti、新 refresh token、新 access token；算新 refresh 的 SHA-256；准备新 value 和**新 key** `user:refresh:<sub>:<new_jti>`
4. **原子轮换**：一次 Redis 调用完成 `compare(oldKey, expected_hash) → DEL oldKey → SET newKey newValue EX ttl`，由 Lua 脚本保证：
   - 旧 hash 不匹配 → 整体不动，返回 mismatch
   - 旧 key 不存在 → 整体不动，返回 mismatch（防过期/已轮换的 token 二次使用）
   - 校验通过 → 旧 key 删除 + 新 key 写入 + 新 TTL 设置，单事务原子提交
5. 任何 mismatch 路径返回 401 INVALID_TOKEN；轮换成功才返回新 token

**为什么必须原子**：如果分两步（先查 hash → 后 DEL+SET），并发同时拿到一个旧 refresh 的两个请求都能通过 hash 校验，最后变成两条新 session 都生效，"一次性"refresh 的语义破。Lua 脚本在 Redis 里是单线程执行的，天然原子。

`refresh_store.go` 暴露的最小 API（不再是单纯 Save/Get/Delete，而是面向用例）：

```go
type RefreshStore interface {
    // Save 登录时写入新 session（SETEX）。
    Save(ctx context.Context, uid uint64, jti, tokenHash string, ttl time.Duration) error

    // Rotate 用旧 (jti, hash) 原子换成新 (newJti, newHash, ttl)；
    // 失败（hash 不匹配 / key 不存在）返回 ErrSessionMismatch，调用方转 401。
    Rotate(ctx context.Context, uid uint64, oldJTI, oldHash, newJTI, newHash string, ttl time.Duration) error

    // Revoke 校验 (jti, hash)，匹配则 DEL；不匹配/不存在视为成功（幂等）。
    // 由 Lua 保证 compare-then-DEL 原子。
    Revoke(ctx context.Context, uid uint64, jti, hash string) error
}
```

错误：

| HTTP | code | 触发 |
|---|---|---|
| 400 | `INVALID_REQUEST` | 缺 refresh_token 字段 |
| 401 | `INVALID_TOKEN` | 签名错 / 过期 / typ 不对 / Rotate 返回 mismatch |
| 500 | `INTERNAL` | 其他 |

#### `POST /api/v1/auth/logout`

请求（与 refresh 相同形状，避免新数据约定）：

```json
{ "refresh_token": "eyJ..." }
```

服务端流程：

1. 解析 + 验证 token（同 refresh 步骤 1：签名 + exp + typ == "refresh"）
2. 从 claims 取 `sub`、`jti`、算 `SHA-256(token)`
3. 调 `RefreshStore.Revoke(ctx, sub, jti, hash)`：Lua 内部做 `compare(stored, hash) → DEL`；不匹配/不存在不报错（幂等）
4. 始终返回 204

成功响应 204 (No Content)。

错误：

| HTTP | code | 触发 |
|---|---|---|
| 400 | `INVALID_REQUEST` | 缺字段 / JSON 畸形 |
| 401 | `INVALID_TOKEN` | 签名错 / 过期 / typ 不对（注意：hash 不匹配 / key 不存在不返回 401，走幂等 204） |
| 500 | `INTERNAL` | 其他 |

### 模块结构

```
internal/module/user/
  module.go            // 现有：扩 NewModule 接受 cfg.Auth + redis
  handler.go           // 现有：加 Login / Refresh / Logout
  handler_test.go      // 现有：补对应 HTTP 用例
  service.go           // 现有：加 Login / Refresh / Logout
  service_test.go      // 现有：补对应单测
  dto.go               // 现有：加 LoginRequest / LoginResponse / RefreshRequest
  errors.go            // 现有：加 ErrInvalidCredentials / ErrInvalidToken
  jwt.go               // 新增：HS256 签发 + 验证 + claims 类型
  jwt_test.go          // 新增：签发/验证用例
  refresh_store.go     // 新增：Redis PoolSession 操作（Save/Rotate/Revoke，Lua 原子）
  refresh_store_test.go // 新增：用 alicebob/miniredis 单测，覆盖 Rotate mismatch/并发、Revoke 幂等

internal/store/
  user_store.go        // 现有：加 FindByIdentifier + FindByID + ErrUserNotFound
```

### 安全 & 日志

- **JWT secret 启动校验**：`main.go` 启动时检查 `cfg.Auth.JWTSecret` 长度 ≥ 32，否则 Fatal 退出（避免开发偷懒用弱密钥）。
- **bcrypt 验证使用 `bcrypt.CompareHashAndPassword`**，恒定时间。
- **统一错误**：`/login` 用户不存在 + 密码错都返回 `INVALID_CREDENTIALS`，缓解枚举（与 1.1 `IDENTIFIER_TAKEN` 同定位：降低信息泄露面，非完整防枚举）。
- **被禁用账号实时拦截**：
  - `/login`：bcrypt 通过后追加 `user.Status == UserStatusActive` 校验；不通过统一返 `INVALID_CREDENTIALS`（与"密码错"同响应，不暴露账号状态）
  - `/refresh`：解析 token 后调 `FindByID(uid)` 查最新 status；不通过返 `INVALID_TOKEN`。这样账号下线即时生效，不靠 refresh token 自然过期
- **日志**（`[user]` 前缀）：
  - `login attempt`：记 `identifier_type`（"username"/"phone"/"email"），**不记 identifier 值**，不记 password
  - `login success`：记 `user_id`、`identifier_type`、`jti`（仅前 8 字符）
  - `login fail`：记 `code`（"INVALID_CREDENTIALS" / "INVALID_REQUEST"），**不区分用户存在与否**
  - `refresh success/fail`：记 `user_id`、新旧 `jti` 前缀、`code`
  - `logout`：记 `user_id`、`jti` 前缀
- **token 永不进日志**：access/refresh 都不打。GORM SQL 已用 `ParameterizedQueries=true`，bind 值不进日志（继承 0.4 改造）。
- **不做**：登录速率限制、IP 黑名单、账号锁定。留给后续中间件层统一做。

### 依赖

- 新增：`github.com/golang-jwt/jwt/v5`
- 新增（开发依赖）：`github.com/alicebob/miniredis/v2`（refresh_store_test 用）

不新增配置项（`AuthConfig` 三字段已在 0.2 定义）。

## 实现步骤

1. `go get github.com/golang-jwt/jwt/v5 github.com/alicebob/miniredis/v2`
2. `internal/module/user/jwt.go` + `jwt_test.go`：签发/验证/claims 类型
3. `internal/store/user_store.go`：加 `FindByIdentifier` + `FindByID` + `ErrUserNotFound`
4. `internal/module/user/refresh_store.go` + `refresh_store_test.go`：Save/Rotate/Revoke（Rotate/Revoke 走 Lua 原子；测试覆盖 Rotate mismatch、并发只允许一次成功、Revoke 对 mismatch/不存在幂等）
5. `internal/module/user/dto.go`：扩展请求/响应类型
6. `internal/module/user/errors.go`：加 sentinel
7. `internal/module/user/service.go`：Login / Refresh / Logout
8. `internal/module/user/service_test.go`：补单测
9. `internal/module/user/handler.go`：3 个新 handler + 错误码映射
10. `internal/module/user/handler_test.go`：补 HTTP 用例
11. `internal/module/user/module.go`：`NewModule` 接受 `cfg.Auth` + `redisManager`，`RegisterRoutes` 加 3 条路由
12. `internal/server/router.go`：传 cfg.Auth 和 redisManager
13. `main.go`：启动校验 JWT secret 长度
14. `make doc-lint` + `golangci-lint` + `go test -race`
15. 启动服务，按退出门 curl

## 退出门

- [ ] `go build .` 通过
- [ ] `golangci-lint run ./...` 0 issues
- [ ] `go test -race ./...` 全过（含 jwt / refresh_store / service / handler 新用例）
- [ ] **登录链路**：`curl POST /login` 三种 identifier 各成功一次（200 + access/refresh + 过期时间字段）
- [ ] **错误链路**：用户不存在 401 INVALID_CREDENTIALS；密码错 401 INVALID_CREDENTIALS；空 identifier / 短密码 400
- [ ] **refresh 轮换**：`curl POST /refresh` → 200 + 新 access + 新 refresh；旧 refresh 立即不可再用（401 INVALID_TOKEN）
- [ ] **logout**：`curl POST /logout` → 204；之后旧 refresh 拿去 /refresh 401
- [ ] **logout 幂等**：同一 token 二次调 logout → 仍 204（不暴露 token 真伪信息）
- [ ] **logout 不踢轮换后的新 session**：先 /refresh 拿到新 token；旧 refresh 去 /logout → 命中"旧 jti key 已被 Rotate 删除"分支，Revoke 走 key-not-found → 仍 204；**新 jti 的 key 完全不被触碰**（Revoke 只查旧 jti）
- [ ] **logout 边界文档化**：当前设计中，任何持有**有效未过期 refresh token** 的请求方都能登出该 jti（这是 token-bearer 模型的固有性质——hash 校验由服务端完成，客户端只需持 token 原文）。如果未来要"只允许 token 持有人本人登出"，需引入 access token 绑定（推后到 1.3）
- [ ] **typ 防混用**：access token 拿去 /refresh 返回 401
- [ ] **JWT secret 校验**：`JWT_SECRET=short` 启动应用应 Fatal 退出
- [ ] **日志**：grep 不到 password / 任何 token 字符串
- [ ] **Redis 验证**：登录后 PoolSession（DB3）有 `user:refresh:<uid>:<jti>` key；logout 后该 key 消失

## 同步检查表自查

| 改动 | 必须同时更新 | 落实 |
|---|---|---|
| 加 REST 端点 | router 注册 + handler + API 文档 | router.go + handler.go + 本文档 API 段 |
| 加 Redis 键 | `internal/cache/keys.go` 定义 + 使用方 + 文档 | `keys.go` 已有 `PoolSession`；本子项在 **`internal/cache/keys.go`** 新增 `UserRefreshKey(uid uint64, jti string) string` helper，集中 key pattern；`refresh_store.go` 调用它，不在模块内自定义 pattern（遵循 change-impact 同步规则）|
| 加新模块功能（不是新模块） | router + handler + AGENTS.md / code-map | 收尾扫时更新 |
| 加配置字段 | 无 | AuthConfig 已在 0.2 |
| 加环境变量 | 无 | JWT_SECRET 已在 dev-workflow 表 |
| 加 MQ Topic | 无 | |

R2 新增触发（来自 651a1a5）：

- "完成 plan.md 子项" → 1.2 完成时更新 README + plan + intro/01
- "新业务端点端到端可用" → 更新 router + handler + intro/04 加链路条目（在链路 0 后追加 "登录链路"，或扩展链路 0）

R7 sweep 列表：

- [ ] AGENTS.md 代码地图加 `加登录/refresh/logout → internal/module/user/...`
- [ ] docs/code-map.md 关键文件速查加"JWT 签发"、"Refresh 存储"
- [ ] docs/dev-workflow.md（如需要补 JWT secret 强度提示）
- [ ] docs/plan.md 1.2 行打勾
- [ ] README.md 阶段进度刷新
- [ ] docs/intro/01 当前状态 1.2 标 ✅
- [ ] docs/intro/04 链路条目（建议把链路 0 扩展为"登录-认证完整链路"，含 register + login + refresh + logout）

## Doc-Impact（commit 模板）

```
Doc-Impact: AGENTS.md, README.md, docs/plan.md, docs/code-map.md, docs/dev-workflow.md, docs/intro/01-what-is-vibeshop.md, docs/intro/04-feature-tour.md, docs/features/1.2-user-login.md
```

**关于 `docs/change-impact.md`**：本次不新增同步规则，沿用 651a1a5 引入的现有规则（"完成 plan 子项" + "新业务端点端到端可用"）。如果实现期间发现 refresh/session 有新的高频耦合（例如多个模块都需要主动吊销用户全部 session），再单独走一次 commit 改 `docs/change-impact.md` 并纳入 Doc-Impact。

---

*创建时间：2026-05-17*
*v2 修订：纳入 Codex Plan Review 反馈——logout 改幂等且强制 hash 校验（与 refresh 同强度）；identifier 识别抽 `classifyIdentifier` helper 复用 1.1；refresh 元数据砍掉 UA/IP；Doc-Impact 段说明 change-impact.md 不动的理由。*
*v3 修订：纳入 Round 2 反馈——refresh 轮换 + logout 改 Lua 原子操作（`Rotate` / `Revoke`），杜绝并发产生多个有效 session 与原子性缺口；退出门 logout 那条改成"不踢轮换后的新 session"，并显式声明 token-bearer 模型边界（同 sub 的有效 refresh 可登出该 jti，未来若需要持有人绑定靠 1.3 access token）；`UserRefreshKey` 移到 `internal/cache/keys.go` 集中。*
*v3.1 修订：Round 3 minor 文案统一——模块结构注释 / 实现步骤第 4 步从 Save/Get/Delete 改成 Save/Rotate/Revoke；logout 退出门文案精确化为"旧 jti key 已被 Rotate 删除→ Revoke 走 key-not-found 幂等"。*
*v3.2 修订（实现后回填）：纳入 Code Review v1/v2 反馈——加 `FindByID` 用于 refresh 路径复查 user.Status；Login/Refresh 都加 disabled-user 拦截；Lua 加 `type(parsed.token_hash) ~= "string"` schema 校验；usernameRE 收紧为"含至少一个非数字字符"避免与 phone 数值空间撞值；refactor `normalizeAndValidate` 改调 `classifyIdentifier`。*
